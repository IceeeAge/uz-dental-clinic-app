// IconSize.ts

// Define the type for IconSize
type IconSizeType = {
    xl: number;   // Extra large icon size
    lg: number;   // Large icon size
    md: number;   // Medium icon size
    sm: number;   // Small icon size
    xs: number;   // Extra small icon size
  };
  
  // Define icon sizes
  const IconSize: IconSizeType = {
    xl: 32,
    lg: 24,
    md: 20,
    sm: 16,
    xs: 12
  };
  
  // Export icon sizes
  export default IconSize;
  